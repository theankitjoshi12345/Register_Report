[project]
name = "register-report-backend"
version = "0.1.0"
description = "Django API for daily store reconciliation reports"
requires-python = ">=3.13,<3.14"
dependencies = [
    "Django>=5.2,<5.3",
    "django-environ>=0.14,<0.15",
    "psycopg[binary]>=3.2,<4",
]

[tool.uv]
package = false
